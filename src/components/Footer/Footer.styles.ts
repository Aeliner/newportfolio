import { styled } from 'styled-components';
import tw from 'twin.macro';

export const IMAGE_SIZE = 48;

export const ActionBarContainer = styled.div`
  ${tw`bg-gray-700 w-full h-12 flex relative`}
`;
